<div class="price">
    <span class="starting">
        ${Resource.msg('label.starting_from', 'pricing', null)}
        <isif condition="${set.price.min}"> 
            ${set.price.min.sales.formatted}
        <iselse>
            ${set.price.sales.formatted}
        </isif>
    </span>
</div>
