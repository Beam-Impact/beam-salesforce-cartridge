<div class="price">
    <span class="starting">
        ${Resource.msg('label.starting_from', 'pricing', null)}
        <isif condition="${product.price.min}"> 
            ${product.price.min.list.formatted}
        <iselse>
            ${product.price.list.formatted}
        </isif>
    </span>
</div>
