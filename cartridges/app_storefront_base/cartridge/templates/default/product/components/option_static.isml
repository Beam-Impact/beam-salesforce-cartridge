<isloop items="${option.values}" var="value">
    <isif condition="${value.id === option.selectedValueId}">
        <div class="col-md-10 col-12 product-option">
            <div>${option.name}: [${value.price}] ${value.displayValue}</div>
        </div>
    </isif>
</isloop>
