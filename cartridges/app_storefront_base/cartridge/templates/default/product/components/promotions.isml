<div class="promotions">
    <isif condition="${pdict.product.promotions.length > 0}">
        <isloop items="${pdict.product.promotions}" var="promotion">
            <div class="callout" title="${promotion.details}">
                ${promotion.calloutMsg}
            </div>
        </isloop>
    </isif>
</div>
