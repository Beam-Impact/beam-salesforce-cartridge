<div class="row">
    <isloop items="${slotcontent.content}" var="product" begin="0" end="${end}">
        <isinclude url="${URLUtils.url('Product-ShowTile', 'pid', product.ID, 'swatches', 'true', 'reviews', 'true', 'compare', 'false')}" />
    </isloop>
</div>
