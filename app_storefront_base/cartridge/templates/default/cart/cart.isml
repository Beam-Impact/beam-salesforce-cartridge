<isdecorate template="common/layout/page">
    <isscript>
        var assets = require('~/cartridge/scripts/assets.js');
        assets.addJs('/js/cart.js');
        assets.addCss('/css/cart.css');
    </isscript>

    <div class="container cart-header">
        <div class="row">
            <div class="col-sm-3">
                <a class="continue-shopping-link" href="#" title="Go to My Account">${Resource.msg('cart.continue.shopping.link','cart',null)}</a>
            </div>
            <div class="col-xs-12 col-sm-6 text-xs-center">
                ${pdict.numItems}
            </div>
            <div class="col-sm-3 text-xs-right">
                <span>${Resource.msg('cart.need.help.text','cart',null)}</span> <span class="help-phone-number">${Resource.msg('cart.sample.phone.number','cart',null)}</span>
            </div>
        </div>
        <hr class="no-margin-top">
    </div>


    <isif condition="${pdict.items.length === 0}">
        <div class="container cart-empty">
            <div class="row">
                <div class="col-xs-12 text-xs-center">
                    <h1>${Resource.msg('cart.cartempty','cart',null)}</h1>
                </div>
            </div>
        </div>
    <iselse/>
        <div class="container cart">
            <div class="row">
                <!--product cards-->
                <div class="col-xs-12 col-sm-12 col-md-7 col-lg-8">
                    <isinclude template="cart/cartProductCard" />
                </div>
                <!--totals, and checkout actions-->
                <div class="col-xs-12 col-sm-12 col-md-5 col-lg-4">
                    <isinclude template="cart/cartPromoCode" />
                    <div class="row">
                        <isinclude template="cart/cartShippingMethodSelection" />
                    </div>
                    <isinclude template="cart/cartTotals" />
                    <div class="row">
                        <div class="col-xs-12">
                            <button class="btn btn-block checkout-btn" type="submit" name="submit" value="submit">
                                ${Resource.msg('global.checkout','cart',null)}
                            </button>
                        </div>
                    </div>
                    <br>
                    <div class="row">
                        <div class="col-xs-12">
                            <button class="btn btn-block" type="submit" name="submit" value="submit">
                                Apple Pay
                            </button>
                        </div>
                    </div>
                    <br>
                </div>
            </div>
        </div>
        <isinclude template="cart/cartRemoveProductModal"/>
    </isif>
</isdecorate>
