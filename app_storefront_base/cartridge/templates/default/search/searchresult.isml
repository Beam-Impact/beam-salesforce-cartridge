<isdecorate template="common/layout/page">
    <div class="container">
    	<!--- to display Product IDs --->
        <isloop items="${pdict.productIds}" var="productId" status="loopStatus">
        	${productId}
        </isloop>
        
        <!--- to display relevant Refinement Definition Names --->
        <isloop items="${pdict.refinementNames}" var="refinementName" status="loopStatus">
        	${refinementName}
        </isloop>
        
    </div>
</isdecorate>
