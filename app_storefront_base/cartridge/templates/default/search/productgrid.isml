<isloop items="${pdict.productSearch.productIds}" var="productId">
    <div>
        <isinclude url="${URLUtils.url('Product-ShowTile', 'pid', productId, 'swatches', 'true', 'reviews', 'true', 'compare', 'true')}" />
    </div>
</isloop>
